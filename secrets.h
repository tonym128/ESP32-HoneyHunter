#ifndef _SECRETS
#define _SECRETS
static char *host = (char*)"esp32";
static char *ssid = (char*)"ssid";			 // To avoid having to enter it manually
static char *password = (char*)"password"; // To avoid having to enter it manually

#endif
